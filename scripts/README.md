# Operational scripts

This directory is reserved for production-safe operational utilities used by the Link Hunter rollout. Database backup tooling must never embed or print credentials; it should read connection details from environment variables at runtime.
