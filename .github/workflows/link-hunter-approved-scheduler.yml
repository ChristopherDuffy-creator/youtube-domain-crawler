name: Retired Web Link Hunter Scheduler

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retired state
        run: echo "Web Link Hunter is retired; no crawler or paid-provider work was started."
